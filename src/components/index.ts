/**
 * This file exports the `BaseLayout` component from the `base-layout.tsx` file.
 * The `base-layout.tsx` file contains the base layout component that renders a layout with a sidebar and a main content area.
 * This file is used to export the `BaseLayout` component for use in other parts of the application.
 *
 * @module components/index
 * @exports {React.ComponentType} BaseLayout - The `BaseLayout` component.
 */

/**
 * The `BaseLayout` component is the main layout component for the application.
 * It renders a layout with a sidebar and a main content area.
 *
 * @component
 * @return {React.ReactElement} The rendered `BaseLayout` component.
 */
export { default as BaseLayout } from "./base-layout";
